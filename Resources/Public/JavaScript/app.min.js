function adjustOverlayHeight() {
    var a = $(window).height(), b = a - 40;
    console.log("Content was to " + $(".content-background").height() + " monOverlay: " + b), 
    b > $(".content-background").height() && $(".content-background").height(b), console.log("Resized to " + $(".content-background").height());
}

jQuery(document).ready(function(a) {
    adjustOverlayHeight(), a(window).resize(function() {
        adjustOverlayHeight();
    }), a(".map-canvas").each(function() {
        var b = new GMaps({
            div: a(this).attr("id"),
            lat: a(this).data("latitude"),
            lng: a(this).data("longitude")
        });
        b.addMarker({
            lat: a(this).data("latitude"),
            lng: a(this).data("longitude"),
            title: a(this).data("markertitle"),
            infoWindow: {
                content: "<p>" + a(this).data("infotitle") + "</p>"
            }
        });
    });
});