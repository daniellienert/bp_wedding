function adjustOverlayHeight() {
    var a = $(window).height(), b = a - 40;
    b > $(".content-background").height() && $(".content-background").height(b);
}

function adjustGestDetails() {
    var a = parseInt($("#person-count").val());
    0 == a && (a = 2), $(".guest-detail").each(function(b) {
        a > b ? $(this).show(100) : $(this).hide(100);
    });
}

jQuery(document).ready(function(a) {
    adjustOverlayHeight(), adjustGestDetails(), a(window).resize(function() {
        adjustOverlayHeight();
    }), a(".map-canvas").each(function() {
        var b = new GMaps({
            div: a(this).attr("id"),
            lat: a(this).data("latitude"),
            lng: a(this).data("longitude")
        });
        b.addMarker({
            lat: a(this).data("latitude"),
            lng: a(this).data("longitude"),
            title: a(this).data("markertitle"),
            infoWindow: {
                content: "<p>" + a(this).data("infotitle") + "</p>"
            }
        });
    }), a("#person-count").change(function() {
        adjustGestDetails();
    }), a("#guest-name").change(function() {
        a("#guest-name-0").val(a("#guest-name").val());
    });
});